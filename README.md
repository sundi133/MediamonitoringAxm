# Negative Media Monitoring Tool

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.

supabase functions deploy make-server-211993fd --no-verify-jwt

For env contact info@votal.ai

## Licence MIT
